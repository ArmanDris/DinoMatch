version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
      - "6334:6334"

  app:
    build: .
    platform: linux/amd64
    ports:
      - "5000:5000"
    depends_on:
      - qdrant
